# Client UI

Frontend application that collects user inputs, calls the Ephemeris API, and renders an astrology chart wheel.

Planned stack: React + TypeScript (SVG rendering).
API contract: /docs/api-spec.md.
