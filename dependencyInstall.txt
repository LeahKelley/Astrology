Windows Install Python Virtual Environment (WSL):
sudo apt install python3.12 python3.12-venv python3.12-dev build-essential -y
python3.12 -m venv .venv

Windows (WSL):
source .venv/bin/activate
pip freeze > requirements.txt (Do this whenever you add new dependencies)
pip install -r requirements.txt
deactivate //to get out of virtual environment

Mac Install Python Virtual Environment (Please Verify):
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python@3.12
xcode-select --install

For Mac (Please Verify):
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
deactivate //to get out of virtual environment